apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-cost.fullname" . }}
  labels:
    {{- include "dclaw-cost.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dclaw-cost.selectorLabels" . | nindent 4 }}
